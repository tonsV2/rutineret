apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rutineret.fullname" . }}-test-connection"
  labels:
    {{- include "rutineret.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rutineret.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
